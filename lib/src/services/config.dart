class Config {
  // For Android emulator use 10.0.2.2 instead of localhost
  static const String baseUrl = "http://10.0.2.2:3000";
  // If testing on a real device, change to your computer's LAN IP, e.g. "http://192.168.1.5:3000"
}
